# 제슐렝가이드 개발 에이전트 시스템 설계서

> Claude Code가 "제슐렝가이드(제로페이 맛집 가이드)" 모바일 웹앱을 단계적·반복적으로 구현·확장할 때 따라야 할 작업 계획서.
> 본 문서는 **개념·구조·역할** 수준의 청사진이며, CLAUDE.md / AGENT.md / SKILL.md의 상세 내용은 구현 단계에서 별도 작성한다.

---

## 1. 작업 컨텍스트

### 1.1 배경 및 목적
사내 구성원이 제로페이 가맹 맛집을 직접 추천·평가할 수 있는 모바일 웹 서비스를 구축한다. 본 에이전트 시스템의 **자동화 대상은 "이 서비스 자체의 개발 작업"**이다. 즉, 요구사항 명세 → 데이터 모델 → API → UI → 지도/랭킹 → 테스트 → 배포까지의 일련의 개발 단계를 Claude Code가 표준화된 워크플로우와 검증 패턴에 따라 진행할 수 있도록 한다.

서비스 핵심 가치는 ① 익명 닉네임 기반의 가벼운 참여, ② 추천/리뷰 기반의 큐레이션, ③ 제안 랭킹·심사 랭킹 등 게이미피케이션, ④ 네이버 지도 기반의 위치 탐색·필터링이다.

### 1.2 범위
**포함**: 모바일 웹 프론트엔드(Next.js), API/DB(Next.js Route Handlers + Prisma + Neon Postgres), 익명 가입/로그인(NextAuth Credentials + DB 세션), 가맹점·리뷰 CRUD, 네이버 지도 연동(Geocoding 서버 프록시 포함), 카테고리·평점·리뷰수 기반 필터, 제안/심사 랭킹, 최소 관리자 페이지(/admin), Netlify 배포.

**제외(이번 범위 아님)**: 사내 SSO/이메일 도메인 검증, 결제·예약 연동, 다국어, 관리자 대시보드(추후 확장 포인트).

### 1.3 입출력 정의
| 구분 | 항목 |
|------|------|
| **에이전트 입력** | 기능 요청(자연어), 와이어프레임/스크린샷(있으면), 외부 자료 URL(예: 네이버 지도 API 문서) |
| **에이전트 산출물** | 구현된 코드(레포지토리), 기능 명세서(`/output/specs/`), 검증 리포트(`/output/reports/`), 배포 결과 URL |
| **서비스 입력**(앱이 받는 것) | 닉네임/비밀번호, 가맹점 제안(상호·주소·카테고리·좌표·제로페이 여부), 리뷰(평점·텍스트) |
| **서비스 출력**(앱이 보여주는 것) | 가맹점 리스트/지도, 상세 페이지, 필터·정렬 결과, 랭킹 페이지, 마이페이지 |

### 1.4 제약조건
- **기술 스택 고정**: Next.js(App Router) 풀스택, **Netlify 배포**, **Neon Postgres**(Prisma).
- **데이터 출처**: 사용자 직접 입력만 사용. 외부 가맹점 API·크롤러 미사용.
- **인증 단순화**: 닉네임 + 비밀번호만으로 가입(사내 검증 없음). 익명 게시판 수준의 보안 가정.
- **모바일 우선**: 데스크톱 대응은 부가 목표, 디자인·터치 영역 모두 모바일 기준.
- **네이버 지도 API**: 사용량·키 발급 정책 준수. API 키는 환경변수로만 관리.
- **PII 최소화**: 닉네임 외 개인정보 수집 금지.

### 1.5 용어 정의
- **제안(Suggestion)**: 새 가맹점을 사용자가 추가하는 행위/엔티티.
- **리뷰(Review)**: 등록된 가맹점에 대한 평점·코멘트.
- **제안 랭킹**: 누적 제안 수가 많은 사용자 순위.
- **심사 랭킹**: 작성한 리뷰 수·평균 평점 등 가중 점수가 높은 사용자 순위.
- **MVP**: 본 설계서에 정의된 9단계가 완료된 상태.

---

## 2. 워크플로우 정의

### 2.1 전체 흐름

본 에이전트의 메인 워크플로우는 **MVP 구축 워크플로우(W1)** 와 운영 단계의 **기능 확장 워크플로우(W2)** 두 가지다. 초기엔 W1을 순차 진행하며, 각 Phase 완료 후 사람의 진행 승인(go/no-go)을 받는다. MVP 이후의 기능 추가·수정은 W2를 반복 적용한다.

```
[W1: MVP 구축]
P0 요구사항 확정
  ↓
P1 데이터 모델 설계 → P2 프로젝트 부트스트랩
  ↓
P3 인증 시스템(닉네임/비번)
  ↓
P4 핵심 CRUD (가맹점·리뷰)         ← backend-builder
  ↓
P5 UI 기본 페이지(모바일)            ← frontend-builder
  ↓
P6 네이버 지도 통합                  ← map-integrator
  ↓
P7 랭킹 시스템 → P8 검색·필터
  ↓
P9 통합 테스트 & 배포

[W2: 기능 확장] (운영 단계 반복)
요청 입력 → 영향 분석(LLM) → 분기:
  • UI만 변경     → frontend-builder
  • API 변경      → backend-builder
  • DB 마이그레이션 동반 → backend-builder + db-migration 스킬
  • 지도 관련     → map-integrator
→ 검증(빌드·테스트) → PR/배포
```

### 2.2 단계별 정의

각 Phase는 **입력 / 책임 주체(메인 vs 서브) / 성공 기준 / 검증 방법 / 실패 처리** 다섯 항목을 갖는다.

#### P0. 요구사항 확정
- **입력**: 사용자 자연어 요청, 본 설계서.
- **주체**: 메인 에이전트.
- **LLM 판단 영역**: 우선순위 결정, 요구사항 충돌·모호점 식별, 비즈니스 룰 추론.
- **산출물**: `/output/specs/requirements.md` (기능 목록·우선순위·수용 기준).
- **성공 기준**: 모든 핵심 기능에 수용 기준(acceptance criteria) 명시, 미해결 모호점 0개 또는 명시적으로 "추후 결정" 라벨링.
- **검증 방법**: LLM 자기 검증(체크리스트) + **사람 최종 승인**(고위험 단계).
- **실패 처리**: 모호점이 검증을 통과 못하면 **에스컬레이션**(사람에게 질문).

#### P1. 데이터 모델 설계
- **입력**: requirements.md.
- **주체**: 메인 에이전트.
- **LLM 판단**: 엔티티·관계 도출, 정규화 수준 결정, 인덱스 후보 식별.
- **코드 처리**: Prisma schema validate, 마이그레이션 SQL 생성.
- **산출물**: `/prisma/schema.prisma`, `/output/specs/data-model.md`(ERD·근거).
- **성공 기준**: requirements의 모든 기능을 표현 가능, `prisma validate` 통과, 외래키·유니크 제약 누락 없음.
- **검증 방법**: 스키마 검증(도구) + LLM 자기 검증(요구사항 ↔ 모델 매핑 표).
- **실패 처리**: validate 실패 → **자동 재시도(최대 3회)**. 매핑 누락 발견 → **에스컬레이션**.

#### P2. 프로젝트 부트스트랩
- **입력**: 기술 스택 결정.
- **주체**: 메인 에이전트(스크립트 위주).
- **코드 처리**: `create-next-app`, ESLint/Prettier/Husky 설정, Tailwind 또는 동등 디자인 시스템 초기화, Prisma 연결.
- **산출물**: 동작하는 빈 Next.js 프로젝트, `.env.example`.
- **성공 기준**: `pnpm build` 성공, `pnpm lint` 무경고, 환경변수 누락 시 명확한 에러.
- **검증 방법**: 규칙 기반(빌드·린트 통과 여부, 도구: `build-verifier`).
- **실패 처리**: 빌드/린트 실패 → **자동 재시도(최대 2회)** → 에스컬레이션.

#### P3. 인증 시스템 (닉네임/비밀번호)
- **입력**: 인증 정책(닉네임 유니크, 비밀번호 해싱).
- **주체**: backend-builder 서브에이전트.
- **LLM 판단**: 자체 구현 vs NextAuth Credentials Provider 채택 결정(근거 기록), 세션 저장 전략(JWT vs DB 세션).
- **코드 처리**: bcrypt 해싱, 세션 쿠키, 보호 라우트 미들웨어.
- **산출물**: 가입/로그인/로그아웃 라우트, 세션 미들웨어, `/output/specs/auth-decision.md`(선택 근거).
- **성공 기준**: 가입→로그인→보호 라우트 접근→로그아웃 시나리오 정상, 비밀번호 평문 저장 0건, 세션 만료 동작.
- **검증 방법**: 단위 테스트 + 통합 테스트(`test-runner` 스킬) + 보안 체크리스트(LLM 자기 검증).
- **실패 처리**: 테스트 실패 → 자동 재시도(최대 3회) → 에스컬레이션. 보안 항목 실패 → **즉시 에스컬레이션**.

#### P4. 핵심 CRUD (가맹점·리뷰)
- **입력**: 데이터 모델, 요구사항.
- **주체**: backend-builder.
- **LLM 판단**: 입력 검증 규칙 도출(Zod 스키마), 권한 정책(본인 글만 수정/삭제).
- **코드 처리**: Route Handler, Prisma 쿼리, 페이지네이션, `schema-validator` 스킬로 입출력 검증.
- **산출물**: `/app/api/places/*`, `/app/api/reviews/*`, OpenAPI 스타일 요약(`/output/specs/api.md`).
- **성공 기준**: 모든 엔드포인트의 정상·예외 케이스 통과, 입력 검증 100%, 권한 우회 0건.
- **검증 방법**: API 통합 테스트 + 스키마 검증.
- **실패 처리**: 자동 재시도(최대 3회) → 에스컬레이션.

#### P5. UI 기본 페이지(모바일)
- **입력**: 기능 명세, 와이어프레임(있으면).
- **주체**: frontend-builder.
- **LLM 판단**: 컴포넌트 분해, 상태 관리 범위(서버 컴포넌트 vs 클라이언트), 모바일 인터랙션 패턴.
- **코드 처리**: 반응형 레이아웃, 폼 검증, 라우팅.
- **산출물**: 가맹점 목록/상세/작성, 리뷰 작성, 마이페이지, 랭킹 페이지(데이터는 P7에서 결합).
- **성공 기준**: 360px~430px 폭에서 깨짐 없음, 키보드/터치 모두 조작 가능, 빌드 통과.
- **검증 방법**: 빌드 통과(규칙 기반) + 시각적 검토(스크린샷 캡처) + LLM 자기 검증(접근성·일관성 체크리스트).
- **실패 처리**: 자동 재시도(최대 2회) → 에스컬레이션. 시각적 부적합은 사람 확인 필수.

#### P6. 네이버 지도 통합
- **입력**: 가맹점 좌표 데이터, 네이버 지도 API 키.
- **주체**: map-integrator.
- **LLM 판단**: 마커 클러스터링 임계값, 지도 ↔ 리스트 동기화 UX.
- **코드 처리**: 지도 SDK 로드, 마커 렌더링, 필터 ↔ 마커 갱신, `naver-map-helper` 스킬의 가이드 참조.
- **산출물**: `/app/map/*`, 지도 컴포넌트.
- **성공 기준**: 가맹점 목록과 지도 마커 일치, 필터 적용 시 마커 갱신, 모바일 줌·드래그 정상.
- **검증 방법**: 통합 테스트 + 수동 검증(사람 확인).
- **실패 처리**: SDK 오류 → 자동 재시도. UX 부적합 → 에스컬레이션.

#### P7. 랭킹 시스템
- **입력**: 사용자/리뷰/제안 데이터.
- **주체**: backend-builder + 메인 에이전트(설계 결정).
- **LLM 판단**: 제안 랭킹·심사 랭킹의 점수 공식(가중치) 결정, 캐싱 주기 결정.
- **코드 처리**: 집계 쿼리, 캐시(메모리/ISR), 랭킹 API.
- **산출물**: `/app/api/rankings/*`, 랭킹 페이지 데이터 결합, `/output/specs/ranking-formula.md`.
- **성공 기준**: 동일 입력에 대해 결정적(deterministic) 결과, p95 응답 시간 < 500ms(시드 데이터 기준).
- **검증 방법**: 단위 테스트(공식) + 데이터 정합성 테스트.
- **실패 처리**: 자동 재시도(최대 2회).

#### P8. 검색·필터
- **입력**: 필터 명세(카테고리·평점·리뷰수·정렬).
- **주체**: backend-builder + frontend-builder.
- **LLM 판단**: 인덱스 추가 필요성, 쿼리 합성 전략.
- **코드 처리**: 쿼리 파라미터 파싱, Prisma where 합성, UI 필터 바.
- **산출물**: 필터 UI, 검색/필터 API 확장.
- **성공 기준**: 모든 필터 조합이 정상 동작, URL 공유 시 동일 결과 재현.
- **검증 방법**: E2E 테스트 핵심 시나리오 통과.
- **실패 처리**: 자동 재시도 → 에스컬레이션.

#### P9. 통합 테스트 & 배포
- **입력**: 완성된 코드베이스, 환경변수.
- **주체**: 메인 에이전트.
- **코드 처리**: 빌드, 환경변수 설정, Vercel/Netlify 배포, 도메인 연결.
- **산출물**: 배포된 URL, `/output/reports/deployment-{date}.md`.
- **성공 기준**: 프로덕션에서 가입→제안→리뷰→지도→랭킹 전 시나리오 통과.
- **검증 방법**: 배포 후 스모크 테스트 + **사람 최종 검토**(외부 노출 단계).
- **실패 처리**: 배포 실패 → 자동 재시도(최대 2회) → 에스컬레이션. 스모크 테스트 실패 → **즉시 에스컬레이션**(롤백).

### 2.3 LLM 판단 영역 vs 코드 처리 영역

| LLM이 직접 수행 | 스크립트(스킬)로 처리 |
|---|---|
| 요구사항 모호점 식별·우선순위 결정 | Prisma migrate / validate |
| 데이터 모델 도출 (엔티티·관계·정규화) | 빌드/린트/타입체크 실행 |
| API 입력 검증 규칙 결정 (Zod 스키마 설계) | 테스트 실행 및 결과 파싱 |
| 컴포넌트 분해·상태 경계 결정 | API 입출력 스키마 검증(런타임) |
| 랭킹 공식·가중치 결정 | 환경변수 점검·배포 트리거 |
| 보안·UX 자기 검증(체크리스트 평가) | 가맹점 좌표 형식 검증·정규화 |
| 네이버 지도 통합 UX 결정 | 로그·리포트 파일 기록 |

### 2.4 분기 조건 및 상태 전이
- **Phase 종료 조건**: 성공 기준 충족 + 검증 통과 + (해당 시) 사람 승인.
- **재시도 조건**: 검증 실패 사유가 (a) 빌드/린트/타입 오류, (b) 단순 누락·형식 오류, (c) 일시적 외부 오류일 때.
- **에스컬레이션 조건**: 보안 위반, 모호한 요구사항, 사람 승인 필요 단계, 재시도 횟수 초과, 외부 노출 직전 단계.
- **스킵 조건**: 선택적 단계(예: 시각적 자동 검증) 환경 부재 시 사유를 `/output/logs/skipped.log`에 기록.

### 2.5 검증 패턴 매핑

| 단계 | 주 검증 유형 | 보조 검증 |
|------|------------|-----------|
| P0 요구사항 | 사람 검토 | LLM 자기 검증 |
| P1 데이터 모델 | 스키마 검증 | LLM 자기 검증 |
| P2 부트스트랩 | 규칙 기반(빌드·린트) | — |
| P3 인증 | 규칙 기반(테스트) | LLM 자기 검증(보안 체크리스트) |
| P4 CRUD | 스키마 검증 + 규칙 기반 | LLM 자기 검증 |
| P5 UI | 규칙 기반(빌드) | LLM 자기 검증 + 사람 검토(시각) |
| P6 지도 | 규칙 기반(통합 테스트) | 사람 검토 |
| P7 랭킹 | 규칙 기반(공식 단위 테스트) | LLM 자기 검증(공식 합리성) |
| P8 필터 | 규칙 기반(E2E) | — |
| P9 배포 | 규칙 기반(스모크) | **사람 검토(필수)** |

---

## 3. 구현 스펙 (구조·역할 정의 수준)

### 3.1 폴더 구조

```
/jelin-guide
  ├── CLAUDE.md                          # 메인 에이전트 지침(오케스트레이터)
  ├── /.claude
  │   ├── /skills
  │   │   ├── /schema-validator
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # Zod 스키마 ↔ 입출력 검증
  │   │   ├── /db-migration
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # prisma migrate / validate 래퍼
  │   │   ├── /build-verifier
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # build/lint/typecheck 실행 + 리포트
  │   │   ├── /test-runner
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # 테스트 실행 + 실패 사례 추출
  │   │   └── /naver-map-helper
  │   │       ├── SKILL.md
  │   │       ├── /scripts               # 좌표 정규화 등
  │   │       └── /references            # 네이버 지도 API 가이드 발췌
  │   └── /agents
  │       ├── /backend-builder/AGENT.md  # API·DB·인증 전담
  │       ├── /frontend-builder/AGENT.md # 모바일 UI 전담
  │       └── /map-integrator/AGENT.md   # 네이버 지도 통합 전담
  ├── /output
  │   ├── /specs                         # 단계별 명세 산출물
  │   ├── /reports                       # 검증·테스트 결과
  │   └── /logs                          # skipped/escalated 로그
  ├── /docs                              # 외부 참고 자료
  ├── /app                               # Next.js App Router
  ├── /prisma
  ├── /components
  ├── /lib
  └── package.json
```

### 3.2 에이전트 구조

**메인 에이전트(CLAUDE.md) = 오케스트레이터.**
W1/W2의 단계 진행, Phase 진입·종료 판정, 서브에이전트 호출, 사람 에스컬레이션 결정을 담당한다. 서브에이전트 간 직접 호출은 금지하며 모든 흐름은 메인을 경유한다.

**서브에이전트 3종**으로 분리한다. 분리 근거는 ① 도메인별로 요구되는 지식이 명확히 다름, ② 각 도메인 지침이 길어 메인에 항상 로드 시 컨텍스트 비효율, ③ 운영 단계(W2) 반복 작업에서도 동일하게 재사용 가능.

| 서브에이전트 | 역할 | 트리거 | 입력 | 출력 |
|---|---|---|---|---|
| **backend-builder** | API Route Handler·인증·DB 쿼리·검증 작성 | P3·P4·P7·P8의 서버 작업, W2의 API/DB 변경 | 요구사항·스키마 파일 경로 | 코드 변경 + `/output/reports/backend-{phase}.md` |
| **frontend-builder** | 모바일 UI 컴포넌트·페이지·반응형·접근성 | P5·P8 UI, W2의 UI 변경 | 명세·디자인 자료·API 계약 | 코드 변경 + 스크린샷·검토 리포트 |
| **map-integrator** | 네이버 지도 SDK·마커·클러스터링·필터 연동 | P6, W2의 지도 관련 작업 | 좌표·필터 명세 | 지도 컴포넌트·통합 코드 |

### 3.3 CLAUDE.md 핵심 섹션 목록 (구조만)
1. 프로젝트 개요·목표
2. 사용 기술 스택·제약(고정)
3. 워크플로우 W1·W2 진입 규칙
4. Phase 별 진입/종료 판정 규칙
5. 서브에이전트 호출 정책 및 전달 규약
6. 검증 패턴·실패 처리 규칙(요약)
7. 산출물 저장 규약(`/output/` 하위 경로 매핑)
8. 사람 에스컬레이션 트리거 목록
9. 보안·환경변수 처리 원칙
10. 코드 스타일·커밋 규약

### 3.4 AGENT.md 핵심 섹션 (서브에이전트 공통 구조만)
1. 역할·책임 범위
2. 트리거 조건(어떤 Phase·어떤 요청에서 호출되는가)
3. 입력 인터페이스(파일 경로 vs 인라인)
4. 출력 인터페이스(코드 변경 + 리포트 경로)
5. 참조 스킬 목록
6. 자기 검증 체크리스트
7. 도메인 원칙(각자 다름: 백엔드 보안 / 프론트 모바일 UX / 지도 사용량 정책 등)

### 3.5 스킬 목록 및 트리거

| 스킬 | 역할 | 트리거 조건 |
|---|---|---|
| **schema-validator** | Zod 등으로 API 입력·출력·DB 페이로드 검증 | 신규/변경 API 작성 시, 데이터 모델 변경 시 |
| **db-migration** | Prisma `validate`·`migrate` 래퍼, 결과 진단 | P1·P4·W2의 DB 변경 발생 시 |
| **build-verifier** | `next build`·`lint`·`tsc --noEmit` 실행·실패 항목 요약 | 모든 Phase의 종료 직전 |
| **test-runner** | 단위·통합·E2E 테스트 실행, 실패 케이스를 LLM이 다루기 쉬운 형태로 정제 | P3 이후 모든 Phase의 검증 단계 |
| **naver-map-helper** | 네이버 지도 API 호출 패턴·좌표 변환·예시, `/references`에 발췌 문서 보관 | P6, W2의 지도 관련 작업 |

스킬은 도구(작은 단위), 서브에이전트는 역할(큰 단위)로 분리되며, 한 서브에이전트가 여러 스킬을 사용한다(예: backend-builder는 schema-validator + db-migration + build-verifier + test-runner를 모두 사용).

### 3.6 데이터 전달 패턴
- **파일 기반**(권장 기본값): 단계 산출물·명세·리포트는 `/output/` 하위에 저장, 서브에이전트에는 **경로만 전달**.
- **프롬프트 인라인**: 한 줄짜리 결정값(예: "랭킹 가중치 = 제안 0.6 / 리뷰 0.4")처럼 작고 단순한 데이터.
- **금지**: 대용량 코드·로그·스키마를 인라인으로 전달.

### 3.7 주요 산출물 파일 형식

| 파일 | 형식 | 생성 시점 |
|---|---|---|
| `/output/specs/requirements.md` | Markdown(기능·우선순위·수용 기준) | P0 |
| `/output/specs/data-model.md` | Markdown + Mermaid ERD | P1 |
| `/prisma/schema.prisma` | Prisma DSL | P1 |
| `/output/specs/auth-decision.md` | Markdown(선택 근거) | P3 |
| `/output/specs/api.md` | Markdown(엔드포인트 요약) | P4 |
| `/output/specs/ranking-formula.md` | Markdown(공식·가중치·근거) | P7 |
| `/output/reports/{phase}-verification.md` | Markdown(검증 통과 항목·실패·재시도 이력) | 각 Phase 종료 |
| `/output/logs/skipped.log` | JSONL | 발생 시 |
| `/output/logs/escalated.log` | JSONL(사람 확인 사유·시점) | 발생 시 |
| `/output/reports/deployment-{date}.md` | Markdown(URL·환경변수·스모크 결과) | P9 |

---

## 4. 확장 포인트 / 알려진 위험

| 항목 | 현재 결정 | 향후 확장 방향 |
|---|---|---|
| 사내 구성원 검증 | 검증 없음(닉네임/비번만) | 이메일 도메인 검증, SSO, 초대 코드 |
| 어뷰징(중복 제안·악성 리뷰) | 사용자 신고 없음, 자동 탐지 없음 | 신고 기능, 동일 IP/계정 빈도 제한, LLM 모더레이션 |
| 가맹점 데이터 신뢰성 | 사용자 자가 신고(제로페이 여부) | 제로페이 공공 API 대조, 관리자 확정 워크플로우 |
| 관리자 기능 | **최소 /admin (env 비번 보호) — 신고·탈퇴·시드·수정이력 롤백** | 별도 어드민 라우트 + 정식 권한 모델 |
| 신고·자동 숨김 | **신고 N건(임계 3) 누적 시 자동 숨김 + 운영자 검토** | LLM 모더레이션 / 자동 제한 / IP·계정 빈도 제한 |
| 다국어 | 한국어 전용 | i18n 도입(향후) |
| 지도 API 사용량 | 단일 환경변수 키 | 키 로테이션·서버 프록시·캐시 |
| 비밀번호 정책 | 최소 길이만 | 약한 비밀번호 차단·재시도 제한·이메일 복구는 의도적으로 미도입(익명성 우선) |

---

## 5. 다음 단계

이 설계서가 확정되면 다음 순서로 구현에 착수한다.

1. 본 설계서를 레포지토리 루트의 `/docs/agent-design.md`로 커밋. (6장 인터뷰 확정 결정 포함)
2. CLAUDE.md, 서브에이전트 3종 AGENT.md, 스킬 5종 SKILL.md의 **상세 내용**을 작성 (본 설계서가 그 골격을 정의하며, 6장이 구체값을 공급함).
3. P0(요구사항 확정)부터 W1을 순차 진행. 단, P0의 1차 입력은 6장이 그대로 사용된다.

---

## 6. 인터뷰 확정 결정 (2026-04-28)

본 섹션은 설계서 1차 작성 후 **단일 의사결정자(운영자)** 인터뷰로 확정된 결정 사항을 한눈에 정리한다. 1~5장의 추상적·"LLM 판단" 영역을 구체화하며, 충돌 시 **본 섹션의 결정이 우선**한다. P0 요구사항 명세(`/output/specs/requirements.md`)의 1차 입력으로 사용한다.

### 6.1 의사결정·일정·성공 지표
- **의사결정 구조**: 단일 의사결정자(운영자) 1인. 다른 이해관계자 없음. Claude Code의 모든 에스컬레이션은 1인에게 향한다.
- **MVP 일정**: 1주 내 번제·수정. P별 사람 승인은 빠르게 한 번에 일괄 처리(P0/P9만 명시 게이트).
- **성공·실패 지표**: 별도로 두지 않음. 사내 파일럿이 "돌아가면 OK". W2 운영 단계는 요청 입력에 반응하는 형태.
- **공개 범위**: 공개 웹. URL을 아는 누구나 가입·제안·리뷰. 사내 SSO·이메일 검증 없음 — 설계서의 "익명 게시판 수준 보안 가정"과 일치.

### 6.2 기술 기반
- **호스팅**: Netlify.
- **DB**: Neon Postgres. 프로덕션·로컬 개발 모두 Neon — 브랜치 기능으로 개발용 스키마 분기. SQLite 이중 스택 미사용.
- **인증**: NextAuth Credentials Provider. **세션 전략은 JWT** (NextAuth v4 제약 — Credentials는 JWT strategy만 지원). DB 세션의 즉시 무효화 효과는 `jwt` 콜백에서 매 요청 시 `User.deletedAt`을 DB에서 재확인하여 동등하게 보장. 비밀번호 bcrypt 해싱.
- **비밀번호 분실**: 복구 불가 명시. 가입 화면에 "복구 수단 없음 — 자주 쓰는 기기에 저장하도록 권장" 경고 노출.
- **모니터링**: Sentry 에러 트래킹 + Netlify Analytics 기본 트래픽. PostHog·Plausible은 PII 최소화 원칙으로 미도입.

### 6.3 데이터 모델·엔티티
- **가맹점 필드**: 필수 — 상호·주소·카테고리·좌표·제로페이 여부 / 메모(선택) — 대표 메뉴·가격대.
- **카테고리**: 고정 enum — `한식 / 양식 / 일식 / 중식 / 분식 / 카페 / 주점 / 기타`.
- **평점**: 3차원 5점 — 맛·가성비·분위기. 평균 평점은 차원별로 저장하고 표시 시 단순 평균.
- **닉네임**: 사용자 자유 입력 + 동명 신접 시 자동 접미사 충돌 회피(예: `구아저씨`, `구아저씨_2`).
- **가맹점 좌표 확보**: 네이버 Geocoding API 서버 프록시 자동 변환. API 키는 환경변수, 서버에서만 호출.
- **중복 가맹점**: 서버에서 주소+상호 유사도 임계값 자동 차단. UX — "이미 등록된 가게입니다. 리뷰로 가시겠어요?".
- **사진**: MVP는 텍스트+평점만. 스토리지·CDN·모더레이션 부담 0. 향후 확장 포인트.

### 6.4 UI / UX
- **디자인 톤**: 미니멀 — 토스·에어비앤비 느낌. Pretendard, B&W 베이스 + 1개 압세색.
- **첫 화면**: 랭킹/큐레이션 카드 우선. 단, 데이터 부족 시 **부트스트랩 모드**로 전환 — "첫 제안자가 되어보세요" CTA.
- **시드 정책**: 출시 직전 운영자가 시드 가맹점 5~10개를 은임하게 등록(전용 계정·식별 닉으로 구분). 지도·랭킹 완전 비움 회피.
- **비회원 접근**: 가맹점·리뷰·지도·랭킹 모두 **로그인 없이 열람 가능**. 쓰기(제안·리뷰·신고)만 로그인 필요.
- **필터·정렬·검색**: 상단 스크롤 칩(카테고리·자주 쓰는 정렬) + 별도 "필터" 버튼 → Bottom Sheet 고급 필터.
- **페이지네이션**: 하이브리드 — 모바일 무한 스크롤, PC/태블릿 페이지 번호.
- **데스크톱 정책**: 태블릿(768px)까지 편안한 반응형, 그 이상은 중앙 정렬 모바일 레이아웃.
- **마이페이지**: 내가 제안한 가맹점 / 내가 쓴 리뷰 / 내 랭킹 위치·추이 / 설정(닉네임 접미사 변경·비번 변경·탈퇴).
- **PWA**: 사용 안 함. 일반 모바일 웹.
- **공유 / OG**: 별도 OG 미구현. 외부 공유는 **네이버 지도 링크**로 대체.
- **알림**: 없음. 서비스 정체성 — "단순 검색·기록 도구".

### 6.5 지도
- **클러스터링**: 마커 50개 이상이면 자동 클러스터링, 줄임 시 해제. 네이버 SDK MarkerClustering 활용.
- **지도-리스트 동기화**: 화면 영역(viewport) 변경 시 리스트도 같이 갱신. 필터 적용 시 마커·리스트 동시 갱신.

### 6.6 랭킹
- **심사 랭킹**: 하이브리드 — `이달의 심사위원`(월간) + `명예의 전당`(전 기간 누적). 화면에서 두 보드 시각적으로 분리.
- **공식**: P7에서 결정·`/output/specs/ranking-formula.md`에 기록. 단순 합산 + 시간 윈도우(월/전 기간) 분리. 디케이 미적용.
- **제안 랭킹**: 누적 제안 수(중복 자동 차단 후) 단일.
- **수정·삭제와의 일관성**: 리뷰 자유 수정·삭제이므로 랭킹은 **현재 활성 리뷰 기준 재계산**. 캐시 주기는 P7에서 결정.

### 6.7 모더레이션·신뢰
- **어뷰징 자동 제한 / LLM 모더레이션**: MVP에서 모두 미적용. 소규모 사내 파일럿 가정.
- **신고**: 모든 리뷰·가맹점에 신고 버튼. 신고 N건(임계 3) 이상 누적 시 항목 자동 숨김 → 운영자 검토 후 복원/삭제.
- **제로페이 가맹 표시**: 자가신고 + 사용자 투표("사용 가능 / 불가") 다수결 표시. 외부 API 미사용. "주장·미확인" 라벨링.

### 6.8 수정·삭제·탈퇴
- **가맹점 정보 수정**: 위키 스타일 — 아무나 수정 가능, 모든 수정 이력 보존.
- **vandalism 방어**: 롤백 버튼(이전 버전 1클릭 복원) + 수정 이력 노출. 자동 정지·이의제기 창 미적용. 사내 30~50명 규모에서 충분.
- **리뷰 수정·삭제**: 언제든 자유. 표시 라벨 미부착(수정 이력 비공개).
- **탈퇴**: 계정만 사라지고 제안·리뷰는 남으며 원저자 닉네임 그대로 표시. 가입·탈퇴 화면에 명시.

### 6.9 관리자(/admin)
- 설계서 1차의 "관리자 기능 제외"를 부분 해제. **최소 관리자 페이지 (/admin)** 둠.
- **보호**: 환경변수 비밀번호 1단계(BASIC_AUTH_USER / BASIC_AUTH_PASS). 별도 권한 모델 미도입.
- **기능**: 신고 대시보드 / 탈퇴 처리 / 시드 가맹점 추가 / 수정 이력 조회·롤백.

### 6.10 테스트·CI
- **자동화 범위**: 핵심 로직 단위 테스트만 — 랭킹 공식, 주소·닉네임 검증, 권한 체크.
- **통합·E2E**: 미작성. 회귀 감지는 빌드(`build-verifier`)·타입체크·수동 QA로 대체.
- **CI**: Netlify 자동 배포 트리거 + GitHub Actions 빌드/린트/단위 테스트.

### 6.11 본 결정의 영향 (설계서 충돌·추가 메모)
- **2.2 P1**: 카테고리 enum / 평점 3차원 / 가맹점 필드 셋이 데이터 모델 입력으로 확정.
- **2.2 P3**: NextAuth Credentials + DB 세션이 결정값. `/output/specs/auth-decision.md`는 결정 근거 정리용 형식만 유지.
- **2.2 P4**: 위키식 수정·자동 중복 차단·신고 임계 자동 숨김이 권한·검증 정책에 반영.
- **2.2 P5**: 디자인 톤(미니멀)·페이지네이션 하이브리드·태블릿 768 대응 명시. 부트스트랩 모드 분기 포함.
- **2.2 P6**: Geocoding 서버 프록시 + 클러스터링 임계 50.
- **2.2 P7**: 하이브리드 랭킹(월간 + 전 기간) 두 보드.
- **2.2 P9**: 시드 5~10개 등록을 배포 직전 운영자 작업으로 명시. /admin 페이지 배포 포함.
- **3.2 에이전트 구조**: 변경 없음(메인 + backend / frontend / map-integrator 3종 유지).
- **3.5 스킬 목록**: `naver-map-helper` 스킬에 Geocoding 서버 프록시 가이드 포함 / `schema-validator`에 카테고리 enum·평점 3차원 schema 포함 / `db-migration`은 Neon 브랜치 흐름 가정.
- **4. 확장 포인트**: "관리자 기능"·"어뷰징" 두 행은 본 결정에 따라 4장 표 갱신됨.

